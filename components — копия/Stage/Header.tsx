export default function Header() {
    return (
        <header className="fixed top-8 left-0 right-0 z-[var(--z-scene)] flex justify-center pointer-events-none">
            <h1 className="text-3xl md:text-5xl font-bold uppercase tracking-[0.2em] text-white/90 drop-shadow-2xl">
                Your Vibe
            </h1>
        </header>
    );
}
